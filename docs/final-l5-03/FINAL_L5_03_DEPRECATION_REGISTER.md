# FINAL-L5-03 — Deprecation Register

| Path/system | Reason retained | Canonical replacement | Consumers remaining | Removal condition | Target sprint | Risk |
|---|---|---|---|---|---|---|
| `tenant_wallets` table + `ProviderCreditWalletService` + `/v1/provider/wallet*` endpoints | Real, working code exists; other consumers besides the one fixed this sprint still call it | `tenant_billing.credit_balance` + `usage_credit_ledger` via `usageCreditsApi` | `frontend/tenant-portal`: `finance/package/page.tsx`, `finance/security-deposit/page.tsx`, `provider/wallet/page.tsx` (3 pages, not migrated this sprint — `TenantLayout.tsx`, the site-wide-impact consumer, was migrated) | All 3 remaining consumers migrated to `usageCreditsApi`; `tenant_wallets` table confirmed to have zero real dependents | Recommended for L5-04 or a dedicated backend-cleanup sprint | Low — endpoint already fails for every real tenant, so migrating consumers only fixes currently-broken pages |
| Legacy `/v1/jobs` (field_ops) | Super-admin's platform-wide job-oversight pages (`admin/operations/*`) still use it — a genuinely different, cross-tenant domain from Tenant Portal's `service_jobs` | `/v1/provider/my-records/jobs` (tenant-scoped only — cannot serve super-admin's cross-tenant need without a new endpoint) | `super-admin/lib/api.ts`'s `jobsApi` (3 page consumers) | A new cross-tenant `service_jobs` admin endpoint would need to exist first | Not scheduled (documented in FINAL-L5-02B already) | Low — legitimate different-domain usage |
| Duplicate `EditBtn`/`DeleteBtn`/`ViewBtn`/`MoreBtn`/`RowActions`/`DataTable` (per-app `ui.tsx`, 0-consumer in at least one app) | FINAL-L5-00 flagged `REVIEW_REQUIRED`, not `DELETE_CONFIRMED`; not re-verified this sprint | N/A (delete if truly 0 consumers after a full JSX call-site check) | See Dead Code Cleanup Report | A second confirmation pass across all JSX call sites (including aliased imports) | Not scheduled | Very low (cheap, small components) |
| 3 independent `components/shared/ui.tsx` files | Real, already-diverged per-app usage (see Shared UI Component Report) | A shared `@serviceos/ui` package (does not exist) | All 3 apps | Would need a monorepo workspace tooling decision first | Not scheduled — large infrastructure change | Medium (touches every import in 3 apps) |
| 3 independent `apiFetch` client implementations | Different token/tenant models per app (justified duplication, rule 5) | Same reasoning as above | All 3 apps | Same as above | Not scheduled | Medium |
| Backend: 500 responses missing CORS headers | Root cause understood (middleware ordering) but fix requires full-endpoint regression testing | Correct `CORSMiddleware` positioning (outermost) | Every backend endpoint (latent, only visible on unhandled 500s) | A dedicated sprint with full endpoint regression coverage | Not scheduled | Medium-high — touches all endpoints, security-sensitive |
| `super-admin/hooks/usePermissions.ts` — real but under-adopted (3/many pages) | No time this sprint to retrofit every admin mutation-gating site | Wider adoption of the existing hook | Most super-admin pages still use no UI-level permission gating (backend remains authoritative regardless) | A dedicated UX-consistency pass | Not scheduled | Low — backend authorization unaffected either way |
| "Bargain Rules" (`admin/pricing/bargain-rules`) | Already self-labeled `[Deprecated]` in its own UI; superseded feature, not removed from the codebase/nav | Automatic Low/Mid/High price options | Whatever tenants/admins still reference existing bargain-rule records | Confirm 0 active bargain-rule records exist, then remove the page/nav entry | Not scheduled | Low |
| `python-json-logger` (backend `requirements.txt`) | Zero grep-evidence of use (FINAL-L5-00), but not independently re-verified this sprint | `structlog`'s own JSON renderer (already used everywhere) | Possibly a deploy-time log-shipping config not visible to source grep | A fresh, independent verification pass before removal | Not scheduled | Very low |

Machine-readable version: `deprecation-register.json`.
