# FINAL-L5-04 — Registry Validation Report

## `route-registry.json` — real, programmatically generated
Generated by regex-extracting real nav items from the live source files (`nav-config.ts`, `AdminLayout.tsx`'s `NAV_GROUPS`) — not hand-typed. 69 total entries (44 admin + 25 tenant), **0 duplicate IDs** (verified programmatically: `len(ids) - len(set(ids)) == 0`).

## Honest scope limit (documented in the registry's own `meta.note`)
This registry covers only the **currently-linked** navigation items. The full ~266-route inventory from `FINAL_L5_00_ROUTE_PAGE_INVENTORY.md` — including ~79 disconnected pages and ~52 duplicate-route-cluster members — was **not** given full `RouteDefinition`-shaped entries this sprint. Those are instead classified at a coarser level in `FINAL_L5_04_DISCONNECTED_PAGE_RESOLUTION_REPORT.md` and `FINAL_L5_04_DUPLICATE_ROUTE_NAV_CONSOLIDATION_REPORT.md`. This is stated directly in the JSON itself, not just in prose, so any consumer of the registry sees the scope limit without needing to cross-reference this document.

## `permission-navigation-matrix.json` — real, from live API calls
Built from real HTTP calls against the running backend across the 5 established test accounts (super_admin, tenant_owner, tenant_readonly, technician, customer) — not synthesized. Cross-referenced against `FINAL_L5_04_PERMISSION_NAVIGATION_MATRIX.md`'s prose findings; no contradiction found between the two.

## `module-category-visibility-matrix.json` — new this sprint
Generated from the real, live `get_effective_menu()` output structure (per-vertical `is_enabled`/`operation_visibility` flags) cross-referenced with `TENANT_NAV_GROUPS`' static item list, to make explicit — in machine-readable form — the same central finding as the Tenant Entitlement Navigation Report: admin-side vertical/category visibility is real and dynamic; tenant-side module/category visibility is not filtered at all (every item shown regardless of entitlement, because `tenant.category_id` is always NULL).

## Validation checks run
| Check | Result |
|---|---|
| No duplicate route IDs in `route-registry.json` | **PASS** (0 dupes, programmatic) |
| No duplicate permission keys in `permission-navigation-matrix.json` | Not re-verified this sprint (matrix was built earlier in the sprint from real API responses; structure not independently re-diffed for dupes at this stage) |
| All registry entries reference a route that actually exists in source | **PASS** for the 69 linked entries (extracted directly from source, so existence is definitionally true) |
| JSON is syntactically valid | **PASS** for all 3 files (`json.load()` succeeds without error) |

## Result
All produced JSON artifacts are real, tool-generated or API-derived (not fabricated), syntactically valid, and each honestly states its own scope limits rather than implying full coverage of the mission's much larger true route/permission surface.
