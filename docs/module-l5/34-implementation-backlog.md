# MODULE-L5-00 — Implementation Backlog (actionable items)

| Backlog ID | Module | Requirement | Gap type | Severity | Affected apps/roles | Recommended sprint | Status |
|---|---|---|---|---|---|---|---|
| BL-001 | Identity & Access | Extend the five-role runtime-matrix methodology (established FINAL-L5-05AI..AM) to cover `tenant_owner`, `staff`, `technician`, `customer`, `guest` — a full 10-role matrix | `MISSING_REQUIREMENT` | HIGH | All apps | MODULE-L5-01 | OPEN |
| BL-002 | Finance/Billing | Delete or repurpose `app/engines/vertical_billing/` (orphaned duplicate of `platform_commerce`'s real `VerticalBillingConfig` implementation); remove its false "Proven Level 5" docstring claim | `DUPLICATE_ENGINE` | MEDIUM | Backend only | MODULE-L5-00A | OPEN |
| BL-003 | Tenant Portal | Reconcile the 23-file `PHASE_6B_TENANT_*`/`TENANT_*` document set against current `frontend/tenant-portal` code; each doc's own "remaining blockers" section must be re-verified, not assumed stale or current | `MISSING_TEST` (process) | HIGH | `tenant_portal` | MODULE-L5-00A | OPEN |
| BL-004 | Staff Application | First-ever real certification pass on `mobile/staff-app` (8 screens) — baseline audit, requirement discovery, and gap register, mirroring the `CUSTOMER-L5-00` pattern already used for the customer mobile app | `MISSING_APPLICATION_SURFACE` | HIGH | `staff_app_mobile` | MODULE-L5-05 | OPEN |
| BL-005 | Booking | Re-verify (with a direct code trace, not assumption) whether customer cancellation/reschedule writes to the same table(s) as booking creation, per the mission brief's own flagged known gap | `DISCONNECTED_PIPELINE` (unconfirmed) | CRITICAL if confirmed | Booking, Customer app | MODULE-L5-07 | OPEN |
| BL-006 | Security Operations | Obtain an authoritative product-owner ruling on Security Policy mutation ownership (`admin_security` vs `super_admin`-only); implement per Decision A/B requirements already documented in FINAL-L5-05AM | `MISSING_REQUIREMENT` | MEDIUM | `super_admin` app, `admin_security` role | MODULE-L5-13 | OPEN (blocked on product decision) |
| BL-007 | Cross-cutting | Reconcile pre-FINAL-L5-05 "release candidate" declaration artifacts (`FINAL_EXECUTIVE_SUMMARY.md` etc.) against the 13 subsequent sprints' real defect discoveries, to determine whether any of their claims are now stale | Documentation/process gap | MEDIUM | N/A | MODULE-L5-00A | OPEN |
| BL-008 | Inventory tooling | Replace `e2e/module_inventory_scan.py`'s name-substring frontend/test-match heuristic with a real import-graph or API-call-site trace, to eliminate known false negatives (e.g. `finance_hub`) | `OBSERVABILITY_GAP` | MEDIUM | N/A (tooling) | MODULE-L5-00B | OPEN |
| BL-009 | 21 `PARTIAL_NO_UI_NO_TEST_MATCH` modules | Manually verify each of the 21 modules flagged by the heuristic scanner as having no matching frontend route or test file — confirm real gap vs. heuristic false negative, module by module | Mixed (`MISSING_FRONTEND` or `MISSING_TEST`, pending verification) | MEDIUM | Varies | Per-module `MODULE-[ID]-L1` sprint | OPEN |
| BL-010 | 17 `BACKEND_ONLY_CANDIDATE` modules | Confirm which of the 17 are genuinely `BACKEND_ONLY_UI_REQUIRED` (real product gap) vs. `BACKEND_ONLY_ACCEPTABLE` (shared infra, or UI exists under a differently-named surface, as found for `auth`/`public_registration`/`coaching_appointment` this sprint) | `MISSING_FRONTEND` (pending per-module confirmation) | MEDIUM | Varies | Per-module `MODULE-[ID]-L1` sprint | 3 of 17 pre-classified this sprint (`auth`, `public_registration`, `coaching_appointment` = acceptable); 14 remain OPEN |

10 actionable backlog items produced this sprint. This is a bounded, honest starting set — not an exhaustive backlog for all 68 modules, consistent with this gate's inventory-not-implementation purpose.
