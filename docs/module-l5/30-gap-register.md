# MODULE-L5-00 — Gap Register

Real, evidence-based gaps found or reconfirmed this sprint. Each has a severity, gap type, and recommended future sprint. This is a bounded, honest subset — not exhaustive across all 68 modules (that depth is out of scope for an L00 gate; see `03-module-registry.md`'s own stated limitation).

| ID | Gap | Severity | Type | Module(s)/App(s) | Evidence | Recommended sprint |
|---|---|---|---|---|---|---|
| MODULE-L5-00-001 | Platform's real role set is 10 roles (`tenant_owner`, `staff`, `technician`, `customer`, `guest` never covered), but the entire FINAL-L5-05 chain (13 sprints) only ever certified the 5 `admin_*` roles | HIGH | `MISSING_REQUIREMENT` (scope gap, not a code defect) | Cross-cutting | `app/core/permissions.py` role-bundle count; FINAL-L5-05 chain's own docs, all scoped to Super Admin | `MODULE-L5-01` (Identity/Access, full 10-role matrix) |
| MODULE-L5-00-002 | `vertical_billing` engine directory is an orphaned duplicate scaffold whose docstring falsely claims "Proven Level 5"; real implementation lives in `platform_commerce` | MEDIUM | `PLACEHOLDER` + `DUPLICATE_ENGINE` | `app/engines/vertical_billing/` | Direct inspection: no router, no model; `VerticalBillingConfig` found only in `platform_commerce/billing_*.py` | `MODULE-L5-00A` (cleanup) or fold into a Finance/Billing module sprint |
| MODULE-L5-00-003 | Two disjoint, never-reconciled Tenant Portal certification efforts exist: the 23-file `PHASE_6B_TENANT_*`/`TENANT_*` set and (implicitly) zero coverage from the FINAL-L5-05 chain, which never touched `frontend/tenant-portal` | HIGH | `MISSING_TEST` / documentation gap | `tenant_portal` | `docs/PHASE_6B_*`, `docs/TENANT_*` (23 files, several self-titled "remaining blockers") | `MODULE-L5-0X` (Tenant Portal reconciliation + gap closure) |
| MODULE-L5-00-004 | `staff_app_mobile` (`mobile/staff-app`) has never appeared in any prior L5-series inventory; only 8 screens found — least-certified application on the platform | HIGH | `MISSING_APPLICATION_SURFACE` (relative to certification depth, not existence) | `staff_app_mobile` | `find mobile/staff-app -iname "*screen*.tsx"` = 8; zero references in `docs/final-l5-05/*` or `docs/customer-app/*` | Early module sprint — Staff app is required for the canonical Job Lifecycle workflow (dispatch/inspection/completion) per this program's own Part 11 |
| MODULE-L5-00-005 | Customer cancellation/reschedule reported (per this mission's own known-gaps list) as disconnected from the canonical booking pipeline | CRITICAL (if confirmed) | `DISCONNECTED_PIPELINE` | Booking / Customer app | Carried forward from mission brief; **not independently re-verified this sprint** (bounded scope) | Next booking-focused module sprint must re-verify with direct code trace before treating as closed or open |
| MODULE-L5-00-006 | Security Policy mutation ownership (`admin_security` vs `super_admin`-only) remains explicitly unresolved pending product-owner decision | MEDIUM | `MISSING_REQUIREMENT` (product decision, not code defect) | Security Operations | `FINAL_L5_05AM_*` cert doc; `L5-05AL-002`/`L5-05AM-001` bug-register entries | Carries forward unchanged until a product owner rules; do not silently resolve |
| MODULE-L5-00-007 | Pre-05 "release candidate" artifacts (`FINAL_EXECUTIVE_SUMMARY.md`, `FINAL_RELEASE_DECISION.md`, etc.) describe an apparent full-platform release declaration that predates 13 subsequent sprints which found and fixed real defects — relationship never reconciled | MEDIUM | Documentation/process gap | Cross-cutting | `docs/FINAL_*.md` (12 files) vs. `docs/final-l5-05/*` chronology | `MODULE-L5-00A` (dedicated reconciliation pass) |
| MODULE-L5-00-008 | Module-inventory scanner (this sprint's own tooling) initially mis-classified 8 modules as `ROUTER_PRESENT_NO_ENDPOINTS` due to a too-narrow regex assuming the router variable is always literally named `router` | LOW (tooling, already fixed) | Tooling defect | `e2e/module_inventory_scan.py` | First scan run vs. corrected run (see `03-module-registry.md`) | Fixed in this sprint; no future sprint needed |
| MODULE-L5-00-009 | 21 of 68 modules (`PARTIAL_NO_UI_NO_TEST_MATCH`) have real backend implementation but no name-matching frontend route or test file found by this sprint's heuristic scanner — genuinely unknown whether this reflects real gaps or heuristic false negatives (e.g. `finance_hub` is real, tested, and used, but under names that don't string-match `finance_hub`) | MEDIUM | `OBSERVABILITY_GAP` in the inventory tooling itself | 21 modules, full list in `03-module-registry.json` | Direct manual spot-check of `finance_hub` confirms at least 1 false negative | A future `MODULE-L5-00B` should replace the name-substring heuristic with an actual import-graph / API-call-site trace |

## Historical known-gap revalidation (Part 30) — bounded, honest

Per the mission brief's own "known gaps" list, this sprint explicitly did **not** re-verify most items (full re-verification of ~15 historical gaps is a multi-sprint effort in its own right, out of this gate's bounded scope). Status:

| Known gap (from mission brief) | Status this sprint |
|---|---|
| Tenant `/wallet` unresolved | `SUPERSEDED` — already resolved and proven `OUT_OF_SCOPE_PROVEN_FOR_FINAL_L5_05` / transferred to future Tenant Portal certification, per FINAL-L5-05AI; still valid |
| Media/profile-photo endpoint issues | `FIXED_AND_PROVEN` per Phase 0B (`project_phase0b_profile_photo` memory) — not independently re-verified this sprint |
| Customer cancellation/reschedule disconnected | `NOT_RE-VERIFIED` — carried forward as open per MODULE-L5-00-005 above |
| Security Policy ownership | `STILL_OPEN` — confirmed this sprint (MODULE-L5-00-006) |
| All other listed known gaps (tenant menu visibility, provider page layout, admin/tenant login, region/tier, serviceability, catalog filter/search, pricing-rule dropdowns, finance/package model inconsistency, tenant setup wizard, notification/cron wiring, async export worker, Staff/Customer app completeness) | `NOT_RE-VERIFIED` — explicitly out of this sprint's bounded scope; each requires its own investigation in a future module sprint, not a blanket assumption either way |

**This sprint does not claim any of the above are closed unless explicitly marked `FIXED_AND_PROVEN` or `SUPERSEDED` with cited evidence.** Everything else is honestly `NOT_RE-VERIFIED`, which is itself the correct, non-fabricated answer for a bounded inventory gate.
