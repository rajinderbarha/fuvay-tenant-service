# Documentation Corrections (Superseding Slice 2F-15A Claims)

Slice 2F-15A's `CUSTOMER_AUTHORITY_PROVENANCE_CLOSED` and `GLOBAL_COVERAGE_RECONCILED` claims required qualification by this slice's mission. This file records the corrections; 2F-15A's own files are preserved for history, with a superseded notice added to its `approval-gate.md`.

| 2F-15A claim | Correction |
|---|---|
| `CUSTOMER_AUTHORITY_PROVENANCE_CLOSED` asserted without proving the EXACT event used for classification | Corrected/confirmed: this slice traced the exact predicate (`from_status IS NULL AND changed_by_role == "customer"`) and proved via new tests that it is `EXACT_CREATION_EVENT_BY_CUSTOMER`, not merely "any qualifying Booking" — 2F-15A's claim was directionally correct but unproven; it is now proven. |
| Any implicit statement that `changed_by_role` alone (without the `from_status IS NULL` co-filter) proves creation origin | Clarified: `changed_by_role` ALONE never proves creation — it is the CONJUNCTION with `from_status.is_(None)` that identifies the creation event specifically. 2F-15A's code was already correct (the conjunction was always present in the query), but its documentation did not explicitly call out or test this distinction; `exact-customer-origination-predicate.md` now does. |
| No explicit test proved later customer activity (cancellation/reschedule/note) cannot retroactively establish provenance | Closed this gap: `later-customer-activity-test-matrix.csv` and `TestLaterCustomerActivityCannotEstablishProvenance` now directly test and confirm this, rather than relying on code-reading alone. |
| No explicit test proved missing/ambiguous legacy history fails closed | Closed this gap: `TestMissingAmbiguousHistoryFailsClosed` (3 scenarios) now directly proves this via executed test code, not source inspection. |
| `179/220` reported without excluding `booking_preflight` from the canonical CSV itself (it remained a row with a distinct guard status) | Corrected: `booking_preflight` is now physically REMOVED from `tenant-mutation-endpoint-inventory.csv` (not merely relabeled), and the persona classification (`final-booking-persona-matrix.csv`) makes explicit that customer-self-service (3 routes) and platform-internal (1 route) dispositions are tracked SEPARATELY from the tenant-facing X/Y headline, never merged into it. The numeric figure (179/220) is unchanged, but its provenance and precision are now demonstrated, not asserted. |
| No test proved `require_tenant_mutation_permission`'s customer branch is independent of tenant mutation scope (source-reading only) | Closed this gap: `TestCustomerDependencyIndependentOfTenantScope` (4 tests) directly invokes the dependency with customer/tenant/super_admin UserContexts, including an adversarial forged-scope case, proving independence and fail-closed behavior by execution, not inference. |

No claim in 2F-15A is reversed by this slice — every finding above is a *strengthening* (executed proof replacing a previously sound but untested code-reading claim), not a discovered regression.
