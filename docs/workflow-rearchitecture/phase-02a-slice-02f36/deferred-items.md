# Deferred Items

None of the 18 Set A routes or the 24 canonically-added Set B routes
were deferred — every one reached a fully-protected, privacy-closed
state in this slice.

Explicitly out of scope for this slice and therefore not deferred-with-
a-blocker but simply untouched (per the mission's OUT-OF-SCOPE list):

- Slice 2F-37 module selection.
- The 3 Set C routes (unchanged, confirmed by hash match — verifier
  R01/R19).
- N01 domain-integrity remediation backlog.
- Migration 144 application.
- `readonly@demo-ac-services.local` remediation.
- The item/location ownership gap noted in `known-limitations.md`
  (out of this slice's specific held-route scope).
