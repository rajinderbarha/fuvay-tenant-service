# Deferred Items

Explicitly out of scope for this slice, deferred per its own mission:

- Slice 2F-38 (application-wide certification) — not begun.
- Migration 144 application to any database.
- `readonly@demo-ac-services.local` / `manager@demo-ac-services.local` role
  remediation.
- Fine-grained per-slice (2F-35/36/37) commit boundary reconstruction —
  deferred indefinitely; determined not credibly reconstructable from
  available evidence (see `recovery-strategy-decision.md`), not merely
  postponed.
- Investigating or terminating the other process actively committing to
  the shared main worktree — outside this session's authority.
- A dedicated held-registry/Set-C from-scratch recount script — noted as a
  gap in `known-limitations.md`, not built this slice.
