# Documentation Corrections

Slice 2F-31's own documentation (`docs/workflow-rearchitecture/
phase-02a-slice-02f31/`) accurately recorded, at the time it was written,
that 5 routes were blocked pending an allow-list expansion this slice
provides. That record is correct as a point-in-time statement and was NOT
edited — it remains the true history of what 2F-31 achieved and did not
achieve.

The only correction made forward (not backward) is the historical-artifact
restoration described in
[historical-artifact-restoration.md](historical-artifact-restoration.md):
`docs/workflow-rearchitecture/phase-02a-slice-02f21/runtime-reverification.csv`
and the 2F-23 equivalent were restored to their true recorded values after
being incorrectly edited in a prior session; the true fact (6 routes
protected via 2F-31, not 2F-21/2F-23) is now correctly attributed via the
`PROTECTED_BY_LATER_SLICE` test mechanism rather than by rewriting either
historical artifact.

No other historical slice document required correction.
