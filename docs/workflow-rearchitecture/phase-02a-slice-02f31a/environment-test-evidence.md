# Environment / Test Evidence

- Python 3.12.4, pytest 8.4.0, pluggy 1.6.0, anyio 4.14.1, pytest-asyncio
  0.26.0 (unchanged from prior slices — no dependency was added or
  upgraded this slice).
- No new environment variable, config flag, or migration was introduced.
- No database migration was required (no model/schema change — only
  service/route/permission logic).
- Test run platform: Windows (win32), same as prior slices in this
  program.
