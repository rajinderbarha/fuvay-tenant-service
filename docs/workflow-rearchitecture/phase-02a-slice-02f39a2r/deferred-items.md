# Deferred Items

- Resolve `list_api_keys`/`get_api_key`'s newly-found read-path
  cross-tenant gap (or fold it into the standing pricing/read-path
  limitation for a future dedicated privacy slice).
- Continue mounted-route classification for the remaining 149 routes
  across ~28 smaller modules (this was explicitly deferred by the
  review until this slice's defect remediation was complete — now
  unblocked for Slice 2F-39A3).
- A dedicated `verify_2f39a2r.py`.
- A second full-backend-regression run.
- All items already deferred by 2F-39/2F-39A/2F-39A2 remain deferred,
  unchanged.
