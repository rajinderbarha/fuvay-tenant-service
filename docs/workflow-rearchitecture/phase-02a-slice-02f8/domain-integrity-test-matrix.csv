scenario,classification,evidence
Duplicate service enablement (same tenant+master_service_id),DUPLICATE_REJECTED,TENANT_SERVICE_ALREADY_ENABLED (409), pre-existing + uq_ts_tenant_service unique constraint -- TestEnableServiceGuardsUnchanged::test_source_confirms_duplicate_enable_guard
Enable inactive platform master service,DUPLICATE_REJECTED (rejected outright),MASTER_SERVICE_INACTIVE (422) -- pre-existing, re-verified -- test_source_confirms_master_service_active_guard
Enable service in inactive category,INTEGRITY_DEFECT_CONFIRMED_AND_ALREADY_FIXED (pre-existing guard),SERVICE_CATEGORY_INACTIVE (422) -- pre-existing
Enable service without category entitlement,INTEGRITY_DEFECT_CONFIRMED_AND_ALREADY_FIXED (pre-existing guard, FINAL-L5-04B),CATEGORY_NOT_ENTITLED (403) -- pre-existing, re-verified -- test_source_confirms_category_entitlement_guard
Foreign tenant_id query-param override on enable/disable,INTEGRITY_DEFECT_CONFIRMED_AND_FIXED_THIS_SLICE,_require_tenant_id now rejects a mismatched tenant_id for non-platform actors -- TestCrossTenantTenantIdOverrideRejected
Foreign tenant_service_id on update/types/brands/pricing/publish/save-draft,DUPLICATE_REJECTED (rejected as not-found),_assert_tenant_owns_ts (pre-existing, MODULE-L5-03) -- TestCrossTenantServiceOwnershipRejection
Repeated enable (re-enabling a disabled service),IDEMPOTENT,enable_service's re-enable path flips is_enabled back to True on the existing row rather than duplicating
Repeated disable (disabling an already-disabled service),IDEMPOTENT (effectively -- re-sets already-false flag),disable_service's lookup does not filter on is_enabled, so a second call is a safe no-op
Remove nonexistent mapping (disable a never-enabled service),DUPLICATE_REJECTED,TENANT_SERVICE_NOT_ENABLED (404) -- pre-existing
Parent mapping removal (disable) with active type/brand children,DUPLICATE_ALLOWED (by existing, documented design -- not a defect),disable_service does not cascade to TenantServiceType/TenantServiceBrand rows -- they remain configured and are restored automatically if the service is re-enabled (see catalog-parent-child-integrity.md)
Service/category mismatch,PRODUCT_DECISION_REQUIRED (not independently re-traced to the byte level),not conclusively tested this slice -- no defect evidenced by existing tests either
Invalid service type / invalid brand mapping,PRODUCT_DECISION_REQUIRED (not independently re-traced),same as above -- existing validation pattern, no defect evidenced
Serviceability mapping against an enabled vs. non-enabled tenant service,PRODUCT_DECISION_REQUIRED,see tenant-catalog-enablement-contract.md -- serviceability does not check TenantService.is_enabled at all; not conclusively provable as a defect requiring a serviceability fix
Preview endpoint state before/after (no state exists),N/A,preview_tenant_price_options touches no record
