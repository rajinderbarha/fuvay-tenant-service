# Deferred Items

None of the 3 Set A routes or the 16 canonically-added Set B routes were
deferred — every one reached a fully-protected, ownership-checked state
in this slice (payments' amount-validation gap is a documented
financial-integrity limitation, not a deferred authorization item — the
route IS protected and canonically closed on the authorization
dimension).

Explicitly out of scope for this slice, deferred to future slices per
the mission's OUT-OF-SCOPE list:

- Slice 2F-38 module selection and the application-wide authorization
  certification decision (not claimed or attempted this slice).
- The 20 Set C routes (unchanged, confirmed by hash match).
- N01 domain-integrity remediation backlog — frozen per the actual
  2F-34 contract, not this slice's to remediate.
- Migration 144 application.
- `readonly@demo-ac-services.local` remediation.
- The pricing GET-by-zone/rule-id read-path scoping gap noted in
  `known-limitations.md` (out of this slice's mutation-only scope).
