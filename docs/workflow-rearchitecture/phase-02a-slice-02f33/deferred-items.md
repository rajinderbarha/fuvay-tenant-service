# Deferred Items

- `update_zone`/`get_zone` tenant-scoping (Set C, explicitly out of scope).
- Zone-deletion dependency/referential-integrity policy decision (see
  [product-decisions-required.md](product-decisions-required.md)).
- Audit-log emission for geo mutations.
- Live two-tenant database integration tests (WS13 exclusion).
- The N01 domain-integrity backlog (unchanged, still open, tracked
  separately per Slice 2F-31A/2F-32 — not touched this slice).
- Selection of the next authorization module (explicitly out of scope —
  this mission does not select one).
