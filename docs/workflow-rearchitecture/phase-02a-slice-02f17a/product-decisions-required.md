# Product Decisions Required

Carried forward, unchanged, from Slice 2F-17 (no new product decision was surfaced by this slice's full-application sweep, since zero new modules/routes were discovered):

1. **`compliance.provider_router`'s `generate_export` rate limiting/throttling.**
2. **`package_commerce.tenant_router`'s `tenant_purchase_package` entitlement/idempotency semantics.**
3. **Bundling small modules into future slices** (`application-wide-module-queue.csv` suggests several groupings — left to future slice planning).

No new product decision is required by this slice's own findings.
