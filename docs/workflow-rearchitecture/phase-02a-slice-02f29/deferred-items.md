# Deferred Items - Slice 2F-29

- The remaining 33 canonical unprotected routes (modules M02-M11).
- The 59 held candidates: still
  `PENDING_INDEPENDENT_OR_MODULE_LEVEL_ADJUDICATION`.
- The separate `/v1/security/api-keys/*` and
  `/v1/security/sessions/{session_id}/revoke` subsystem (a distinct module).
- The two architecture/product observations in known-limitations (duplicate
  email across tenants; already-inactive deactivation semantics).
- Next module selection - explicitly out of scope for this slice.
