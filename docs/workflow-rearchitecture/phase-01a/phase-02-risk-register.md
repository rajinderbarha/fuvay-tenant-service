# Phase 2 Risk Register

| # | Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|---|
| 1 | Booking Exception workspace gets built against the wrong record — CONFIRMED risk, not hypothetical: verification proved Booking/field_ops Job/ServiceJob are all 3 actively used in production today | High (confirmed) | High — would recreate the exact fragmentation this initiative fixes | Workflow formally BLOCKED per booking-exception-final-contract.md; do not schedule in Phase 2 at all, not even provisionally |
| 2 | Two admin review-stack pages both continue accepting writes after "canonical" is picked | Medium | High — silent data divergence between `reviews` and `customer_reviews` tables | Verification pass (in progress) must confirm legacy `review/router.py` write capability; if writable, block new writes at the API layer before Phase 2 UI ships, not after |
| 3 | Chat domain decision (Decision 4) not fully resolved by Phase 2 start | High | Medium | Ship Customers > Chat pointing at whichever engine current frontends actually call (per verification pass); treat as COMPATIBILITY_READ_ONLY / SUPPORTING until formal decision, not a blocker for the rest of Phase 2 |
| 4 | ~~Security deposit backend replacement never materializes~~ RESOLVED — verification confirmed finance_hub's live replacement | N/A | N/A | No longer a risk; ship Deposits tab normally |
| 5 | `require_super_admin` gating gap causes admin_operations/finance/security/readonly roles to see broken/empty nav items in Phase 2's new role-specific shells | Medium | Medium | Nav must degrade gracefully — hide items those roles cannot reach rather than showing them a 403; do not attempt the backend permission-wiring fix within Phase 2's frontend-only scope |
| 6 | Consolidating jobs/bookings/appointments/service-jobs into one Jobs list surfaces stale/duplicate rows if Decision 1 isn't fully closed | Medium | High | Gate the merged Jobs list behind Decision 1 too (same as the Exception workspace) — do not merge the list ahead of the underlying model decision |
| 7 | Duplicate setup-template/customer-flow engines cause the Setup Wizard's step 1/2 to route to different backend behavior for different tenants (if both are actually live) | Low-Medium | Medium | Ship wizard without templating feature until reconciliation confirmed; verify which of the two customer-flow engines the current booking flow calls before wiring step 1 |
| 8 | New aggregation endpoints (My Work, setup-progress) become a second source of truth that drifts from the underlying records | Low | Medium | Enforce as strictly read-only/derived at query time, no caching layer that could go stale beyond a short TTL (per aggregation-endpoint-recommendations.md) |
| 9 | Retiring nav entries for confirmed duplicates breaks a saved bookmark/deep link a real user relies on | Low | Low | Use redirects, not hard removal, for every RETIRE-disposition route |
| 10 | Mobile staff-app parity gap becomes a support complaint once web Profile workspace visibly diverges further | Medium | Low | Explicitly out of Phase 2 scope per phase-02-scope.md; communicate this to stakeholders now so it isn't a surprise |
