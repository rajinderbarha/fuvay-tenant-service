# Slice 2F-15C Approval Gate

## Final status

**SECURITY_DOMAIN_INTEGRITY_AND_PRIVACY_CLOSED_PRODUCT_POLICY_BLOCKED**

- **SECURITY_CLOSED**: all Booking routes remain classified (`final-booking-route-persona-table.csv`, single persona per route) and protected. Customer and tenant dependency behavior remains explicit (`customer-dependency-regression.md`, unchanged, re-verified). Runtime verification exits 0 for `booking.router` (11/11, with 1 false-positive exemption), `field_ops.router` (28/28), `field_ops.staff_router` (6/6).
- **CUSTOMER_AUTHORITY_PROVENANCE_CLOSED**: creation provenance is tied to the exact creation event (`from_status IS NULL`, unchanged from 2F-15B) AND the creation actor's ID now equals the Booking's own customer ID (`changed_by == Booking.customer_id`, new this slice) — closing the theoretical gap where role equality alone could not prove actor-to-customer identity. Actor account validity is satisfied transitively via `get_current_user`'s authentication requirement (documented, `known-limitations.md`). Initial status is validated structurally (`initial-status-semantics.md`: exactly one writer, one `to_status` value, `PENDING_CONFIRMATION`). Conflicting/duplicate creation events are proven, by matrix and test, to fail closed in every examined category (`conflicting-creation-history-matrix.csv`). Later customer actions still cannot establish origin (unchanged from 2F-15B). Missing/ambiguous legacy provenance still fails closed (unchanged from 2F-15B, re-verified with the tightened predicate). Untrusted Bookings cannot establish relationship, be directly referenced by `create_job`, or convert to Job — all re-verified with the actor-bound predicate.
- **DOMAIN_INTEGRITY_CLOSED**: provenance checks occur before relationship use and conversion in every examined code path (unchanged source positions). Invalid provenance creates no partial records (`no-partial-persistence-proof.md`). Existing Booking state and conversion rules remain intact; `Booking`/`ServiceBooking` and `field_ops.Job`/`ServiceJob` remain separate.
- **PRIVACY_CLOSED**: a different customer's history cannot legitimize a Booking (the actor-binding fix is the direct proof of this — `changed_by == Booking.customer_id` excludes any other customer's creation event by construction). Ambiguous legacy records cannot affect a different customer's history (every query is scoped to the target `tenant_id`+`customer_id`). Errors remain uniform (`CUSTOMER_TENANT_RELATIONSHIP_REQUIRED` across every denial category, unchanged). Audit actor identities remain accurate (append-only `BookingStatusHistory`, `changed_by`/`changed_by_role` always server-derived).
- **GLOBAL_COVERAGE_CLOSED**: X/Y (**175/216**) now contains tenant/provider mutations ONLY — the 3 customer routes and 1 platform route are physically excluded from the canonical CSV, not merely relabeled. Customer mutations (3, all protected) and platform/internal mutations (1, protected) are reported separately, never merged into X/Y. Both canonical CSVs recount identically (`test_canonical_totals`, updated). Runtime inventory agrees (`tenant_denominator_count: 6`, matches exactly).
- **PRODUCT_POLICY BLOCKED** for: recovery of missing-history legacy Bookings; historical creation-actor account-state re-validation; tenant-local customer directory; verified invitation/consent workflow; new provenance columns; legacy-data remediation migration; database-level uniqueness constraint on the creation-history row; database concurrency hardening; Booking/ServiceBooking consolidation; frontend implementation. None of these are security gaps in the code as it exists today.

## Scope discipline confirmed

No provenance column, migration, customer directory, or invitation/OTP/consent workflow was created. No merge of `Booking`/`ServiceBooking` or `field_ops.Job`/`ServiceJob`. `PartsRequest` and `quote_checklist` untouched. No role, alias, or permission added. No frontend/UI work performed. `readonly@demo-ac-services.local` confirmed untouched. Migration 144 confirmed unapplied. No visual redesign. No second module was begun — this slice's ONLY code changes were (1) the actor-binding filter added to 4 existing read-only queries, (2) the runtime tool's persona map correction and drift check, and (3) the physical removal of 4 rows from the canonical CSV that never belonged in the tenant-only inventory.

## Coverage

**175 protected of 216** tenant-facing mutation routes, reconciled row-by-row from the last approved 169/210 baseline (`canonical-tenant-coverage-recount.md`). Separately: 3 protected customer-self-service Booking mutations, 1 protected platform/internal Booking mutation, 1 false-positive Booking route excluded entirely.

## Regression

8/8 new tests passing. 4 pre-existing tests updated (mock-shape/assertion corrections, no behavioral change in what's tested). Full broad partition sweep: **1734 passed, 9 skipped, 0 failed** (`regression-report.md`, `test-report.md`).

## Stop condition

Per this slice's closing instruction, this response stops at the Slice 2F-15C approval gate. No further router module is started. `field_ops.router` (28/28) and `field_ops.staff_router` (6/6) are preserved. All previously-approved Slice 2F-15/2F-15A/2F-15B authorization fixes are preserved and re-verified. Every item this slice's mission listed as unapproved is now adjudicated: the creation-origin predicate is bound to `Booking.customer_id` explicitly (not role equality alone); conflicting/duplicate creation-history behavior is documented and proven fail-closed; missing/invalid `changed_by_user_id` fails closed; the canonical coverage figure is corrected to 175/216 with full row-by-row arithmetic from the 169/210 baseline; and the contradictory "customer routes are a subset of tenant-facing routes" claim is retired in favor of exclusive, non-overlapping persona categories.
