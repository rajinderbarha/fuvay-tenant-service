# Documentation Corrections (Superseding Slice 2F-15B Claims)

| 2F-15B claim | Correction |
|---|---|
| Provenance queries proved "exact creation event by customer" using `changed_by_role == "customer"` alone | Corrected: role equality alone never proved the acting user WAS the Booking's own customer. All 4 provenance query sites now additionally require `changed_by == Booking.customer_id` (or the single-booking equivalent). This is a strengthening, not a reversal — the legitimate customer self-booking path already satisfied this binding implicitly (server-derived `customer_id == actor_id`); the fix closes the theoretical gap for any future/alternate creation path. |
| No test proved conflicting/duplicate creation-history rows fail closed | Closed this gap: `conflicting-creation-history-matrix.csv` and `legacy-fail-closed-matrix.csv` enumerate every scenario in the mission's list and confirm fail-closed behavior (mostly inherited from the existing equality-filter design, now made explicit and tested for the actor-binding case specifically). |
| **179/220** canonical coverage reported without recognizing that 2F-15B's persona map counted 3 customer-reachable routes as `TENANT_PROVIDER_MUTATION` while ALSO reporting them as a "customer self-service subset" | Corrected — this was the exact contradiction flagged in this slice's unapproved item #6 ("customer routes are excluded while also being a subset of the tenant-facing Booking count"). Fixed by giving each route exactly ONE persona: the 3 dual-reachable routes are now `CUSTOMER_SELF_SERVICE_MUTATION` and are PHYSICALLY REMOVED from the canonical tenant CSV (not merely relabeled), along with `void_booking` (platform). New canonical figure: **175/216** (down from the incorrect 179/220 — the drop reflects removing 4 rows that never should have counted toward the tenant-facing denominator, not a regression in protection). |
| "Customer routes are a subset of tenant-facing routes" | Corrected — this framing is retired. Customer self-service routes are now a SEPARATE, non-overlapping category, entirely excluded from the tenant-facing X/Y. Their tenant-side reachability (for the 3 dual-permission routes) is tracked as `BOOKING_ALSO_TENANT_REACHABLE_CUSTOMER_ROUTES` for transparency only, never re-merged into the denominator. |
| Any combined tenant/customer/platform denominator terminology | Corrected — `canonical-tenant-coverage-recount.md` and `final-booking-route-persona-table.csv` use single-persona-per-route classification throughout, with three explicitly separate counts (tenant/customer/platform) that are never summed into one headline. |

No code-level authorization decision from 2F-15B is reversed — every finding above is either a query-level hardening (actor-binding) or a bookkeeping/classification correction (persona labels, coverage arithmetic), not a change to who can or cannot perform any action.
