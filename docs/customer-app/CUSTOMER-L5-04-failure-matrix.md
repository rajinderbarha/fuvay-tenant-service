# CUSTOMER-L5-04 — Failure Matrix

| Failure | Detection point | Customer impact | Screen | Message | Retry | Cache behavior | Navigation | Analytics | Log level | Severity |
|---|---|---|---|---|---|---|---|---|---|---|
| Category not found / inactive / not customer-visible | `GET /categories/{id}` → 404 (`CUSTOMER_CATEGORY_NOT_FOUND`/`_INACTIVE`/`_NOT_VISIBLE`) — all three map to the same HTTP status, indistinguishable client-side | Category screen shows generic error | CategoryDetailScreen | "Couldn't load this category" | Yes | Query stays `isError`, no stale data shown | Back navigation available | `category_request_failed` (implicit via error state) | warn | P2 |
| Category has no flow config | 404 (`CUSTOMER_CATEGORY_FLOW_NOT_CONFIGURED`) | Same as above — category exists but is not customer-flow-ready | CategoryDetailScreen | "Couldn't load this category" | Yes | Same | Same | Same | warn | P2 |
| Service missing / inactive | `GET /offerings/{id}` → 404 | Service detail screen shows generic error | ServiceDetailScreen | "Couldn't load this service" | Yes | Same | Back navigation | `service_detail_validation_failed` only on schema mismatch, not on a clean 404 | warn | P2 |
| Service list schema mismatch | zod `safeParse` failure on an individual offering | That one row silently dropped, others still render | CategoryDetailScreen | No customer-visible message for a per-row drop; whole-page failure shows the page-level error state | Yes (page-level only) | Dropped row never cached | N/A | `service_list_items_dropped` | warn | P3 |
| Search timeout / network error | `ApiError` category `timeout`/`network_error` from `apiClient` | Search results show error state | SearchScreen | "Search isn't working right now" | Yes | No stale results shown | N/A | Implicit via `api.error` | warn | P2 |
| Search rate limited (429) | `ApiError` category `rate_limited` | Same generic error path — no distinct 429 message built this sprint (not observed/testable without a live backend) | SearchScreen | "Search isn't working right now" | Yes (manual, no auto-retry storm — React Query default `retry: 1`) | No stale results shown | N/A | Implicit | warn | P3 — see known-gaps |
| No search results | `results.data` present, `categories.length + offerings.length === 0` | Zero-result empty state, never confused with an error | SearchScreen | "No results for \"...\"" + "Browse categories" action | Implicit (re-type) | N/A | "Browse categories" → Home | `search_results_loaded` still fires with a zero count bucket | info | P4 |
| Invalid filter/sort | Not applicable — no filter/sort UI exists to submit an invalid value (see filter-and-sort-contract.md) | N/A | N/A | N/A | N/A | N/A | N/A | N/A | N/A | NOT_APPLICABLE |
| Wrong tenant / wrong marketplace | Not independently testable — these endpoints carry no marketplace/tenant-scoping parameter at all (contract-matrix.md) | N/A this sprint | N/A | N/A | N/A | N/A | N/A | N/A | N/A | See known-gaps — UNVERIFIED, not FAIL |
| Invalid deep link (malformed id, unknown path) | `deep-link-validator.ts` `malformed_param`/`unknown_path` | Link silently rejected, no navigation, no crash | N/A (rejected before any screen mounts) | N/A | N/A | N/A | Falls through to normal in-app navigation | `deep_link.rejected` | info | P3 |
| Offline | `NetInfo`-driven `OfflineBanner` (pre-existing, reused unmodified) | Banner shown; cached data (if any) still renders | All three new screens | "Offline" (existing banner copy) | Retry via pull-to-refresh once reconnected | Existing cached pages remain visible | N/A | N/A | N/A | P3 |
| Session expiry mid-request | 401 → `handleUnauthorized()` refresh coordinator (CUSTOMER-L5-02, unmodified) | Transparent single retry; full logout only if refresh itself fails | Any of the three new screens | Falls through to existing auth flow | Automatic (one retry) then manual re-login if refresh fails | `queryClient.clear()` on forced logout | Redirected to Authentication if refresh fails | Existing `api.error`/auth events | warn | P2 |
| Unsafe image URL | `httpsOrRelativeImageUrl` zod refinement (reused from CUSTOMER-L5-03's category schema) | Falls back to icon rather than rendering an unsafe URL | CategoryDetailScreen | N/A (silent fallback) | N/A | N/A | N/A | N/A | N/A | P3 |
