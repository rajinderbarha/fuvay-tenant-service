# CUSTOMER-L5-11 — Failure Matrix

| Failure | Detection point | Customer impact | Screen state | Message | Retry | Draft/booking reconciliation | Analytics | Log level | Severity |
|---|---|---|---|---|---|---|---|---|---|
| Draft missing/expired/no address/not serviceable/no provider matched | `evaluatePricingPreflight` (reused) | Cannot reach the review screen | preflight_failed state | `pricing.preflight.*` (reused keys) | Navigate to AddressSelection | N/A | N/A | N/A | P2 |
| `/summary` fetch fails | `useBookingReview` mutation error | Cannot see any review data | unavailable state | `bookingReview.unavailableTitle`/`unavailableDescription` | Yes ("Try again") | Draft unaffected | `booking_review_load_failed` | warn | P1 |
| `ready_for_confirmation: false` | Real backend flag | Cannot confirm yet | not_ready state, per-section rows | `bookingReview.notReadyTitle` + specific rows | Edit actions per real missing section | Draft unaffected | N/A | info | NOT_APPLICABLE (correct, honest behavior) |
| `/confirm` fails — certain non-commit (`validation_error`/`not_found`/`forbidden`/`conflict`) | `categorizeConfirmFailure` → `failed` | Booking not created; can retry immediately | failed banner on review screen | `bookingReview.confirmFailedTitle`/`confirmFailedDescription` | Yes (tap Confirm again — same idempotency key) | Draft unaffected (real preflight failures like `SELECTED_PROVIDER_NOT_BOOKABLE` collapse to this generic category — see preflight-contract.md) | `booking_create_failed` | warn | P1 |
| `/confirm` fails — possible commit (`timeout`/`network_error`/`server_error`/`unknown_error`/`maintenance`/`rate_limited`) | `categorizeConfirmFailure` → `uncertain` | Calm "checking" state, no premature failure or success claim | uncertain state | `bookingReview.uncertainTitle`/`uncertainDescription` | "Check status" (same idempotency key, safe per DB-unique guard) | Resolved by the retry itself (returns the real existing booking if one was created) | `booking_create_timeout` | warn | P1 |
| `/confirm` response fails schema validation | `parseConfirmBookingResult` returns `null` | Treated as `validation_error` → `failed` | failed banner | Same generic copy | Yes | Draft unaffected (a real booking may exist server-side if this happened after a genuine success with a malformed body — an edge case the "Check status" flow on a subsequent visit would still resolve via `useBookingDetail`, though this exact path isn't automatically triggered from the `failed` state) | `booking_create_failed` (reason: validation) | warn | P1 |
| `GET /bookings/{id}` fails (network/validation) | `useBookingDetail` query error | Confirmation screen shows a generic unavailable state, even though the booking was genuinely created | unavailable state (on the confirmation screen) | `bookingConfirmation.unavailableTitle`/`unavailableDescription` | Yes (refetch) | Booking exists server-side regardless — this is a display-only failure | `booking_fetch_failed` | warn | P1 |
| `GET /bookings/{id}` returns `{"error": "FINAL_BOOKING_NOT_FOUND"}` | `parseBookingDetailResponse` → `error` kind | Same unavailable state | Same | Same | Yes | N/A — should not occur in the real flow (the ID just came from a successful `/confirm` response) | `booking_fetch_failed` (reason: FINAL_BOOKING_NOT_FOUND) | warn | P1 |
| `GET /bookings/{id}` returns `{"error": "FINAL_ACCESS_DENIED"}` (theoretical, unreachable via real UI) | Same | N/A — no real UI path constructs this | N/A | N/A | N/A | N/A | N/A | N/A | NOT_APPLICABLE |
| Logout/account switch mid-flow | `queryClient.clear()` (CUSTOMER-L5-02 pattern, reused) | Redirected away, no foreign state persists | Redirected to Authentication/BaselineLanding | Existing auth flow copy | N/A | Booking (if already created) is unaffected server-side; cache discarded | N/A | N/A | NOT_APPLICABLE (correct behavior) |
