# CUSTOMER-L5-08 — Failure Matrix

| Failure | Detection point | Customer impact | Screen state | Message | Retry | Reconciliation | Analytics | Log level | Severity |
|---|---|---|---|---|---|---|---|---|---|
| No eligible provider (`HOME_BOOKING_NO_PROVIDER_AVAILABLE`) | `useMatchProvider` mutation error (422, indistinguishable from below — see contract-matrix.md) | Cannot proceed past this stage for the current address | No-match state | `providerMatch.noMatchTitle`/`noMatchDescription` (generic — real backend detail text not reachable, see security-review.md) | Yes ("Try again", re-runs the mutation) | Draft's `provider_match_status` unchanged (backend never wrote it on this failure path) | `provider_match_no_match` | warn | P1 |
| Provider matched but no price range configured (`PRICE_OPTIONS_UNAVAILABLE`) | Same mutation error path, same 422 status | Same as above — this client cannot distinguish the two reasons | Same no-match state | Same generic copy | Same | Same | Same | warn | P1 |
| Match request network/timeout failure | `ApiError` `network_error`/`timeout` category | Same no-match state (this sprint does not add a separate offline-specific UI) | No-match state | Same generic copy | Yes | Unaffected | `provider_match_no_match` | warn | P2 |
| Match response fails schema validation (unexpected shape) | `parseProviderMatchResult` returns `null` inside the mutation function | Same no-match state — fails closed rather than rendering a partially-valid provider | No-match state | Same generic copy | Yes | Draft cache untouched (onSuccess never runs) | `provider_match_failed` (reason: validation) | warn | P1 |
| Unrecognized badge string in an otherwise-valid response | `resolveBadgeTranslationKey` returns `null` | None — badge still renders (raw string) | Success state, unaffected | Raw badge text as sent by backend | N/A | N/A | `provider_badge_unmapped` | warn | P3 |
| Draft not owned by the calling customer (theoretical, unreachable via real UI) | Backend draft-ownership check inside `get_booking_draft` | N/A — no real UI path constructs this | N/A | N/A | N/A | N/A | N/A | N/A | NOT_APPLICABLE |
| Logout/account switch mid-match | `queryClient.clear()` (CUSTOMER-L5-02 pattern, reused) | Redirected away, no foreign state persists | Redirected to Authentication/BaselineLanding | Existing auth flow copy | N/A | Cache discarded | N/A | N/A | NOT_APPLICABLE (correct behavior) |
| Repeated "Try again" taps in quick succession | No debounce added this sprint | Each tap re-runs the real, non-idempotent-but-safe-to-repeat backend call (re-derives, overwrites — see contract-matrix.md) | Loading state shown for each in-flight attempt | N/A | N/A | Last response wins (React Query's default mutation behavior — no explicit request cancellation added) | N/A | N/A | P3 |
