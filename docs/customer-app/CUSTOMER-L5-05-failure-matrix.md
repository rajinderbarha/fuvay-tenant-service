# CUSTOMER-L5-05 — Failure Matrix

| Failure | Detection point | Customer impact | Screen state | Message | Retry | Workflow reconciliation | Answer preservation | Analytics | Log level | Severity |
|---|---|---|---|---|---|---|---|---|---|---|
| Service detail fails to load (entry validation) | `useServiceDetail` `isError` | Cannot start the assistant | Full-page error | "Couldn't load this service" | Yes | N/A — no session created yet | N/A | Implicit via `api.error` | warn | P1 |
| Missing `serviceId`/`categoryId` route params | Guard at top of `BookingAssistantScreen` | Cannot start the assistant | Full-page error | "This service is no longer available" | Exit only | N/A | N/A | N/A | N/A | P2 |
| Issue-types/service-options catalog fails to load | `assistant.catalogsError` | Assistant cannot start | Full-page error | "Couldn't load these questions" | Yes (`assistant.retry()` refetches all needed catalogs) | N/A — session not yet created | N/A | Implicit via `api.error` | warn | P1 |
| Brands/service-types fails to load (when required) | Same, only checked when `requiresBrand`/`requiresType` | Same | Same | Same | Same | Same | Same | Same | warn | P1 |
| Catalog schema validation failure (individual item) | zod `safeParse` failure inside a list | That option silently absent from the list; others still usable | Renderer shows fewer options, no crash | No customer-visible message for a per-item drop | N/A | N/A | N/A | `<catalog>_items_dropped` | warn | P3 |
| Catalog schema validation failure (whole envelope) | `parse*List` returns `null` | Same as full catalog load failure | Full-page error | "Couldn't load these questions" | Yes | N/A | N/A | `<catalog>_validation_failed` | warn | P2 |
| Stale/tampered answer submission | `submitAnswer`'s `stepId` mismatch guard | Assistant shows the unsupported/error state rather than silently corrupting progress | `ErrorState` (unsupported-step path) | "This step isn't available yet" | Exit only | Session enters `ERROR`, no further submission accepted | Prior valid answers remain in `session.answers` (not cleared) | N/A this sprint (no vendor) | N/A | P2 |
| Unsupported/unknown question type | `resolveQuestionRenderer` returns `recognized: false` | Cannot proceed past this step | `ErrorState` | "This step isn't available yet" | Exit only | N/A | Prior answers preserved | `unsupported_question_received` | warn | P2 |
| Duplicate rapid tap on a single-select option | Not specially guarded — `submitAnswer` is idempotent-safe by construction: a second tap after the step has already advanced would submit against a stale `stepId` and fail closed to `ERROR` rather than double-advance | Worst case: assistant halts at the unsupported/error state instead of skipping a step | `ErrorState` | Same as tampering case | Exit only | Session `ERROR` | Preserved | N/A | N/A | P3 |
| App background during a step | React Navigation keeps the screen instance (and `useState` session) alive — no special handling needed | None — session unaffected | Unchanged | N/A | N/A | Unchanged | Preserved | N/A | N/A | NOT_APPLICABLE (no real gap) |
| Session expiry (auth) mid-assistant | Existing CUSTOMER-L5-02 refresh coordinator (401 → refresh → retry, unmodified) | Transparent single retry; full logout only if refresh fails | Assistant screen (retried request) or redirected to Authentication | Existing auth flow copy | Automatic once, then manual | If logout occurs, `queryClient.clear()` + screen unmount discards the session | Lost only if the customer is fully logged out (expected — see security-review.md) | Existing `api.error`/auth events | warn | P1 |
| Offline | Existing `OfflineBanner` pattern was not added to this screen this pass — see known-gaps | A submission attempted offline will fail via the normal `ApiError` `network_error` path (only relevant for the catalog fetches, since no backend answer-submission call exists to fail mid-flight) | No distinct offline banner shown on this screen yet | Generic catalog-load error message | Yes | N/A | Preserved (in-memory, unaffected by network state) | N/A | N/A | P3 |
| Logout during the assistant | `queryClient.clear()` (CUSTOMER-L5-02) + `reevaluateStartup()` redirect | Screen unmounts, session discarded | Redirected to Authentication/BaselineLanding | Existing auth flow copy | N/A | Session discarded, nothing to reconcile | Lost (expected) | N/A | N/A | NOT_APPLICABLE (correct behavior, not a failure) |
| Account switch during the assistant | Same mechanism | Same | Same | Same | N/A | Same | Lost (expected — see isolation tests in known-gaps) | N/A | N/A | NOT_APPLICABLE |
| Malformed payload (invalid JSON) | Caught by `apiClient`'s existing `normalizeApiError` (unmodified) | Same as catalog load failure | Full-page error | Generic error message | Yes | N/A | N/A | Implicit | warn | P2 |
