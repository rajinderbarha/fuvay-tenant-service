# Compliance Pattern

`/dev/ux-03/compliance` — requirement / status
(`not_submitted`/`submitted`/`under_review`/`approved`/`rejected`/
`expiring_soon`/`expired`) / documents / review history, one card per
requirement. History entries are append-only (actor, action, timestamp) —
no edit/delete of past history entries.
