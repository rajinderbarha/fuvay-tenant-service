# Tenant Operational Workspace Pattern

`components/ux03/patterns/OperationalWorkspace.tsx` — 2 or 3 column live-ops
layout (queue / detail / optional side context), each a `Card`. Used by
Assignment/Dispatch; designed to back future live-ops surfaces without
claiming capabilities (e.g. AI matching) not confirmed in the repo.
