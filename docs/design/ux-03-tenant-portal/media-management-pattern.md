# Media Management Pattern

`/dev/ux-03/media` — logo/gallery/job-photo/document kinds. `previewToken`
stands in for whatever safe reference a real API would return; storage
keys, signed URLs, and credentials are never rendered or logged anywhere in
this phase's code. See known-limitations.md for the N01 media-integrity
backlog note (not independently re-verified this phase).
