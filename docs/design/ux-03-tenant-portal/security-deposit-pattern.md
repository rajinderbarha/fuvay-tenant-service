# Security Deposit Pattern

`/dev/ux-03/security-deposit` — status (`not_required` / `pending` /
`held` / `partially_released` / `released` / `forfeited`) + history list.
Rendered entirely separately from Package & Credits and from any job
payment concept — see package-credit-commission-pattern.md.
