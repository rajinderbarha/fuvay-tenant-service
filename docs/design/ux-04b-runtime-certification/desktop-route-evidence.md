# Desktop Route Evidence

`desktop-light` and `desktop-dark` Playwright projects (1280x800 Desktop
Chrome) — 29 tests each, all passing (see `browser-smoke-test-report.csv`
for the full breakdown, `browser-test-framework.md` for the project
config). Covers all 18 showcase routes plus the pipeline-label and
field_ops.Job-section-exclusion checks, plus keyboard-focus and axe scans.
