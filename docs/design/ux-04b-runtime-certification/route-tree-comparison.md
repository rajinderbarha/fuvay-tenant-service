# Route Tree Comparison

UX-04A final build: ~132 routes. UX-04B adds 2 new routes
(`/dev/ux-04/field-ops-job-detail`, `/dev/ux-04/parts-list`), bringing the
total to ~134. No route was removed or renamed. `/dev/ux-04/parts-approval`
kept its path but its fixture content changed (see
`prerequisite-bug-fix-report.md`). All other UX-01/02/03/04/04A routes
unchanged — confirmed via the same full-route `next build` output list
used in `tenant-build-report.md`.
