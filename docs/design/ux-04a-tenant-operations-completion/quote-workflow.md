# Quote Workflow (UX-04A)

No change to `QuoteSummary` or `QuoteView` this pass — still rendered
inline in Job Detail Workspace (item 11, MERGED_WITH_NAMED_WORKFLOW per
`original-scope-reconciliation.csv`). Covered by
`components/ux04/QuoteSummary.tsx` (UX-04 baseline).
