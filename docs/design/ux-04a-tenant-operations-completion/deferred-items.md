# Deferred Items (UX-04A)

- Compliance submission resubmission UI (item 21 extension fields).
- Investigate and fix the UX-03 test hook-call failure.
- Add a lint solution compatible with Next.js 16 (ESLint config +
  `eslint-config-next` directly, since `next lint` no longer exists).
- Per-adapter-method contract documentation table.
- Distinct `ServiceBookingFixture` type (pending the product decision in
  `product-decisions-required.md` item 7).
- Headless-browser-based hydration and theme-toggle verification.
- Real accessibility tooling (`jest-axe` or equivalent).
- Everything still open from UX-04 baseline's own `deferred-items.md` that
  wasn't closed this pass (operational search UI/adapter, filter/preset
  controls on list views, NextActionPanel/EntityLinkCard/ActivityTimeline
  components, `TenantDetailPage` vs bespoke-layout convergence decision).
