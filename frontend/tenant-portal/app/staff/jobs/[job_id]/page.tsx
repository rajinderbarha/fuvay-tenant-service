// Keep the short /staff/jobs/:id URL used by navigation and notifications,
// but render the one complete Home Services execution workspace. The previous
// duplicate page omitted checklists, parts, quote-aware actions, and the
// fixed-price path, which left technicians stranded mid-job.
export { default } from "../../home-services/jobs/[job_id]/page";
