import type{MetadataRoute}from"next";import{cities,services,siteUrl}from"../lib/site";export const dynamic="force-static";export default function sitemap():MetadataRoute.Sitemap{const fixed=["","services","providers","warranty-safety","about","contact","legal"];return[...fixed.map(path=>({url:`${siteUrl}/${path}${path?"/":""}`,changeFrequency:"monthly" as const,priority:path?0.7:1})),...services.map(s=>({url:`${siteUrl}/services/${s.slug}/`,changeFrequency:"monthly" as const,priority:.8})),...cities.map(c=>({url:`${siteUrl}/cities/${c.slug}/`,changeFrequency:"weekly" as const,priority:.8}))]}
