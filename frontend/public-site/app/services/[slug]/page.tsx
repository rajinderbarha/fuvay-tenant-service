import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { BookingChoices } from "../../../components/SiteChrome";
import { services, siteUrl } from "../../../lib/site";
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)return{};return{title:`${s.name} in Punjab`,description:`${s.description} Check PIN-code availability and book through Fuvay.`,alternates:{canonical:`/services/${s.slug}/`}}}
export default async function ServicePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)notFound();const schema={"@context":"https://schema.org","@type":"Service",name:s.name,description:s.description,areaServed:{"@type":"State",name:"Punjab"},provider:{"@type":"Organization",name:"Fuvay",url:siteUrl}};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><section className="page-hero shell"><div className="breadcrumbs"><a href="/">Home</a> / <a href="/services/">Services</a> / {s.name}</div><div className="service-detail"><div><span className="kicker">{s.punjabi}</span><h1>{s.name} in Punjab</h1><p>{s.description}</p><div className="notice"><strong>Availability is checked by PIN code.</strong><br/>You will only see providers who selected this service and are fully bookable in your area.</div><BookingChoices/></div><div className="detail-image"><img src={s.image} alt={s.name}/></div></div></section><section className="section shell"><div className="content-grid"><article className="content-card"><h2>Before you confirm</h2><p>Fuvay shows the matched provider and either one provider-published fixed price or clear inspection terms.</p></article><article className="content-card"><h2>After the work</h2><p>Completion evidence and provider details remain connected to the booking. Eligible completed jobs receive a downloadable warranty certificate.</p></article></div></section></>}
