import type { Metadata } from "next";import Link from "next/link";import {notFound} from "next/navigation";import {BookingChoices} from "../../../components/SiteChrome";import {cities,services} from "../../../lib/site";
export function generateStaticParams(){return cities.map(c=>({slug:c.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const c=cities.find(x=>x.slug===slug);return c?{title:`Home Services in ${c.name}, Punjab`,description:`Book AC and appliance home services in ${c.name} by PIN code through WhatsApp, Instagram or the Fuvay app.`,alternates:{canonical:`/cities/${c.slug}/`}}:{}}
export default async function CityPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const c=cities.find(x=>x.slug===slug);if(!c)notFound();return <><section className="page-hero shell"><span className="kicker">Punjab service area</span><h1>Home services in {c.name}</h1><p>Check your {c.name} PIN code first. Fuvay then shows only the services published by providers who are approved, bookable and serving that area.</p><BookingChoices/></section><section className="section shell"><div className="section-head"><div><span className="kicker">Services people look for</span><h2>Book with a connected service record.</h2></div></div><div className="service-grid">{services.slice(0,6).map(s=><Link className="service-card" href={`/services/${s.slug}/`} key={s.slug}><div className="service-image"><img src={s.image} alt=""/></div><div><h3>{s.name}</h3><p>{s.description}</p><span>Check {c.name} availability →</span></div></Link>)}</div></section></>}
